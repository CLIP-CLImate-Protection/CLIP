import 'package:flutter/material.dart';

class QuestEachPage extends StatelessWidget {
  const QuestEachPage({super.key});

  static const String url = '/quest/each';

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
